#!/bin/bash

src_dir="html-src"

for i in $(find "$src_dir" -type f); do
    output=${i#"$src_dir/"}
    mkdir -p "$(dirname "$output")"
    scripts/parse-file "$i" | scripts/SmartyPants.pl > "$output"
done
